<%= form_for(task) do |f| %>
  <%= f.label :title %>
  <%= f.text_area :title %>
  <%= f.label :details %>
  <%= f.text_area :details %>
  <div>
    <%= f.label :completed %>
    <%= f.check_box :completed, :class => "chckbx_form"%>
  </div>
  <%= f.submit :class => "submit"%>
<% end %>
